# aces-skills

**ACES Skill Repository — Mind Over Metadata LLC**  
Peter Heller · [mindovermetadata.com](https://mindovermetadata.com) · [@QCadjunct](https://github.com/QCadjunct)

---

## What this is

This repository implements the **Three-File Skill Standard** — the operational unit of the ACES (Agentic Content Management System) proof-of-concept.

Every skill in this repo is a directory containing exactly three files:

| File | Role | Generated by |
|------|------|-------------|
| `system.md` | Source of truth — LLM behavioral contract | Navigator (human) |
| `system.yaml` | Machine-readable config — 12 structured fields | `from_system.md_to_system.yaml` |
| `system.toon` | Wire format — Token-Optimized Object Notation | `from_system.md_to_system.toon` |

`system.md` is the single source of truth. `system.yaml` and `system.toon` are **derived artifacts** — never edited directly.

---

## Taxonomy

```
aces-skills/
├── MetaArchitecture/               ← abstract class: defines the contract
│   └── ACES_skill_deployers/
│       └── ACES_skill_deploy_generators/
│           ├── system.md
│           └── deploy_generators.sh    ← nine-step deployment pipeline
│
├── CodingArchitecture/             ← concrete class: HOW patterns
│   └── FabricStitch/
│       └── ACES_extract_wisdom/
│           ├── system.md
│           ├── system.yaml
│           ├── system.toon
│           └── fabric_stitch.sh        ← multi-vendor AI pipeline
│
└── TaskArchitecture/               ← concrete class: WHAT patterns (future)
```

**MetaArchitecture** is an abstract class — it defines the contract that governs both CodingArchitecture and TaskArchitecture.  
**CodingArchitecture** is a concrete class — it implements reusable HOW patterns (vendor-agnostic execution).  
**TaskArchitecture** is a concrete class — it implements domain-specific WHAT patterns (future).

---

## The deployment pipeline

`deploy_generators.sh` implements the nine-step skill deployment pipeline:

```
VALIDATE → RESOLVE → ARCHIVE → GENERATE → WRITE → CONFIRM → DEPLOY → LOG → COST
```

```bash
# Generate system.yaml and system.toon from system.md — deploy to DEV
./deploy_generators.sh \
  --source CodingArchitecture/FabricStitch/ACES_extract_wisdom/system.md \
  --generate all \
  --env dev
```

**Parameters:**

| Parameter | Values | Default |
|-----------|--------|---------|
| `--source` | path to `system.md` | required |
| `--generate` | `yaml` · `toon` · `all` | `all` |
| `--archive` | `true` · `false` | `true` |
| `--env` | `dev` · `qa` · `prod` | `dev` |

**Every run produces:**
- `output/system.yaml` and/or `output/system.toon`
- uuidv7 archive of source `system.md`
- Entry in `~/.config/fabric/deploy_audit.log`
- Cost receipt in `~/.config/fabric/cost_audit.log` (Step 9)

---

## TOON — Token-Optimized Object Notation

TOON is the wire format for ACES skill dispatch. It delivers **~19% token reduction** vs YAML on the same data — validated: 392 vs 482 out tokens on `ACES_extract_wisdom`.

```
# system.yaml (482 out tokens)          # system.toon (392 out tokens)
identity:                                !skill
  name: ACES_extract_wisdom              n:ACES_extract_wisdom
  version: 1.0.0                         v:1.0.0
  domain: CodingArchitecture             d:CodingArchitecture
...                                      ...
```

`model_dump_tool()` is the canonical TOON serialization method in the ACES Pydantic V2 data model.

---

## Cost accounting standard

Every bash utility in this repo includes **cost accounting as Step N** — a standard component, not an afterthought.

**Rate card (per token):**

| Vendor | Model | In | Out |
|--------|-------|----|-----|
| Anthropic | claude-sonnet-4-6 | $0.000003 | $0.000015 |
| Google | gemini-2.0-flash | $0.000000375 | $0.0000015 |
| Ollama (local) | qwen3:8b | $0.000000 | $0.000000 |

**Validated runs:**

| Pipeline | Vendor | Cost |
|----------|--------|------|
| `fabric_stitch.sh` (5 steps) | Gemini + Claude + Ollama | $0.007963 |
| `deploy_generators.sh` (9 steps) | Ollama (local) | $0.000000 |

---

## Flat deploy pattern (ADR-003)

Fabric does not walk `patterns/` recursively. The ACES taxonomy is the source of truth. `deploy_generators.sh` bridges taxonomy to the Fabric flat runtime:

```
SOURCE   patterns_custom/ACES_Skills/CodingArchitecture/FabricStitch/ACES_extract_wisdom/
              ↓ deploy_generators.sh
DEV      patterns_custom/ACES_extract_wisdom/
QA       patterns_qa/ACES_extract_wisdom/
PROD     patterns/ACES_extract_wisdom/
```

---

## Related repos

| Repo | Description |
|------|-------------|
| [QCadjunct/acms-langgraph-poc](https://github.com/QCadjunct/acms-langgraph-poc) | LangGraph execution controller, Marimo monitor, Pydantic V2 data model |
| [QCadjunct/pydantic-v2-essentials](https://github.com/QCadjunct/pydantic-v2-essentials) | TOON implementation — `model_dump_tool()` |

---

## Architecture Decision Records

Seven ADRs govern this repository — documented in  
`Mind-Over-Metadata/10-AI-Agent-Orchestration/ACES-Architecture-Decisions-20260313.md`

| ADR | Decision |
|-----|----------|
| ADR-001 | MetaArchitecture as abstract class |
| ADR-002 | ACES_skill_deployers as coarse container |
| ADR-003 | Option B — Flat deploy pattern |
| ADR-004 | DEV→QA→PROD promotion pipeline |
| ADR-005 | uuidv7 archive versioning |
| ADR-006 | Cost accounting as bash utility standard |
| ADR-007 | Three-file skill standard validated end-to-end |

---

## Prerequisites

```bash
# Fabric (Daniel Miessler)
pip install fabric-ai   # or: brew install fabric

# Ollama (local inference)
ollama pull qwen3:8b

# Set default model
echo "DEFAULT_MODEL=qwen3:8b" >> ~/.config/fabric/.env
```

---

## License

© 2026 Mind Over Metadata LLC — Peter Heller  
All rights reserved. See LICENSE for terms.
